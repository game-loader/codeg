{
  "name": "codeg-zotero-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Zotero 10 plugin for Codeg Academic",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "build": "node scripts/build.cjs"
  }
}
